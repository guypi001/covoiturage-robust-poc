WEBAPP_HOST=app.example.com
TRAEFIK_ACME_EMAIL=ops@example.com

# SMTP (identity service)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=mailer@example.com
SMTP_PASS=change-me
SMTP_FROM="KariGo <noreply@example.com>"
# Set to true to log OTP/reset links instead of sending email
SMTP_LOG_ONLY=false
